apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo3
  namespace: demo
  labels:
    app.kubernetes.io/name: demo3
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: demo3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: demo3
    spec:
      containers:
      - name: demo
        image: gcr.io/google-samples/hello-app:1.0
        ports:
        - containerPort: 8080
